"use client";

import { ComingSoon } from "@/components/coming-soon";

export default function DispatchPage() {
  return (
    <ComingSoon
      title="Dynamic Dispatch Control"
      description="The dedicated Dispatch Panel will allow coordinators to allocate active response units, handle dispatch requests, and configure vehicle telemetry in a future release. Use Live Incidents to manage updates for now."
    />
  );
}
